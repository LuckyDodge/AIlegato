# Models

Put exported model artifacts here.

Expected future layout:

```text
models/
  ailegato-classifier-baseline-v1/
    best.onnx
    preprocessing.json
    metrics.json
```

Large model files are ignored by git. Commit metadata and small reproducibility files, but keep heavy artifacts in release storage or shared drive.
